export type deptList = {
    deptId?: number,
    name: string,
    value?: number
}

export interface formType {
    name?: string,
    havePosition?: number,
    deptId?: number,
    positionName?: string,
    isDimission?: number
}