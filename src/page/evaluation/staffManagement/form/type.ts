export type deptList = {
    deptId?: number,
    name: string,
    value?: number
}

export interface formType {
    name?: string,
    meta?: string,
    dept?: number,
    position?: boolean,
    status?: boolean
}